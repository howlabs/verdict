description = "Toggle xiao judgment layer: on (full), lite (flags only), off"
prompt = "xiao judgment layer toggle: {{args}}. Levels: on = Pre+Post+Stop gate; lite = PostToolUse flags only; off = disabled. Default on if no arg."